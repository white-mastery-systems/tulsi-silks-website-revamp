# ng-connection-service declares outdated Angular 6 peer ranges; library still works on Angular 14+.
# Remove when replaced with a maintained package or when its peers are updated.
legacy-peer-deps=true
