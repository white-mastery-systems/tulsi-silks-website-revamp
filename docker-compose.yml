version: "3.9"

services:
  ecommerce-ssr:
    container_name: tulsisiks
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "4007:4006"
    restart: always
    environment:
      - NODE_ENV=production
    logging:
      options:
        max-size: "10m"
        max-file: "3"
